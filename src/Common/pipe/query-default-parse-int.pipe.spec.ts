import { QueryDefaultParseIntPipe } from './query-default-parse-int.pipe';

describe('QueryDefaultParseIntPipe', () => {
  it('should be defined', () => {
    expect(new QueryDefaultParseIntPipe()).toBeDefined();
  });
});
